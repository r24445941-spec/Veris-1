[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3.11 -m pip install --break-system-packages -r requirements.txt"]

[start]
cmd = "python3.11 -m uvicorn app:app --host 0.0.0.0 --port $PORT"
